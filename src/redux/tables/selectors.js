export const getProductList = (state) => state.table.products;

export const getWarehouseList = (state) => state.table.warehouses;

export const getQuantitiesList = (state) => state.table.quantities;
